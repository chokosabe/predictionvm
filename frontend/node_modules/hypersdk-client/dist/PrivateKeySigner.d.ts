import { SignerIface } from "./types";
import { VMABI } from "./Marshaler";
import { TransactionPayload } from "./types";
export declare class PrivateKeySigner implements SignerIface {
    private privateKey;
    constructor(privateKey: Uint8Array);
    signTx(txPayload: TransactionPayload, abi: VMABI): Promise<Uint8Array>;
    getPublicKey(): Uint8Array;
    connect(): Promise<void>;
    static debugExtractPublicKey(signed: Uint8Array): Uint8Array;
}
export declare class EphemeralSigner extends PrivateKeySigner {
    constructor();
}
