import { SignerIface } from "./types";
import { VMABI } from './Marshaler';
import { TransactionPayload } from './types';
export declare const DEFAULT_SNAP_ID = "npm:hypersdk-snap";
export declare class MetamaskSnapSigner implements SignerIface {
    private snapId;
    private cachedPublicKey;
    constructor(snapId: string);
    getPublicKey(): Uint8Array;
    signTx(txPayload: TransactionPayload, abi: VMABI): Promise<Uint8Array>;
    connect(): Promise<void>;
    reinstallSnap(): Promise<void>;
    private _invokeSnap;
}
